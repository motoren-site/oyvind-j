apiVersion: apps/v1
kind: Deployment
metadata:
  name: oyvind-jo-deployment
  namespace: apps
  labels:
    app: hello
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hello
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
        - name: registry.digitalocean.com/olsen/oyvind-jo
          image: registry.digitalocean.com/olsen/oyvind-jo
          ports:
            - containerPort: 80
